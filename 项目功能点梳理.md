# 项目功能点梳理

## 一、项目概述

**项目名称**：Lab Log（实验室视觉日志系统）
**项目类型**：MVP（最小可行产品）
**核心目标**：通过相机和视觉大模型，自动记录实验室内的人物动作（使用设备和药品等）和设备运转状态，生成结构化文本日志。

**技术栈**：
- 后端：Python (FastAPI, WebSocket, SeekDB)
- Android 客户端：Kotlin (Camera2 API, MediaCodec)
- Web 前端：React + Vite
- 数据库：SeekDB（关系型 + 向量 + 全文搜索）
- AI 模型：Qwen3-VL / Qwen3.5 / Gemini 视觉大模型

---

## 二、系统架构与模块划分

### 2.1 Android 客户端 (`android-app-trial/`)

| 功能模块 | 核心实现 | 说明 |
|---------|---------|------|
| **摄像头预览与采集** | `viewmodel/RecordingViewModel.kt` | Camera2 API，YUV_420_888 格式采集 |
| **视频编码** | `utils/VideoEncoder.kt` | H.264/H.265 硬件编码（MediaCodec），自动回退 |
| **时间戳水印** | `utils/OcrBFontRenderer.kt` | NV12 Y平面绘制，OCR-B 字体，黑色背景白色文字 |
| **二维码识别** | ML Kit 集成 | 实时识别用户二维码，按分段聚合，随视频上报 |
| **WebSocket 通信** | `WebSocketManager` | 与后端流媒体服务器实时通信 |
| **本地存储** | `StorageManager.kt` | 视频、缩略图、元数据本地保存 |
| **配置管理** | `ConfigManager.kt` | 从 assets/config.properties 读取配置 |
| **视频理解** | `VideoUnderstandingService.kt` | 调用阿里云 Qwen3-VL API，SSE 流式输出解析 |

### 2.2 后端 - 流媒体服务器 (`streaming_server/`)

| 功能 | 文件 | 说明 |
|-----|------|------|
| WebSocket 服务 | `server.py` | 接收 Android 端推送的 MP4 分段（Base64 编码） |
| H.264 帧解析 | `h264_parser.py` | 提取 SPS/PPS，IDR 帧处理 |
| FPS 估算 | `server.py` | 基于帧时间戳估算帧率 |
| 录制会话管理 | `server.py` | 分段保存、会话终结、MP4 封装 |
| 二维码结果处理 | `server.py` | 接收并保存二维码识别结果 (`{segment_id}_qr.json`) |
| 监控统计 | `monitoring.py` | 连接数、流量统计 |

### 2.3 后端 - 视频处理 (`video_processing/`)

| 功能 | 文件 | 说明 |
|-----|------|------|
| **视频理解处理器** | `qwen3_vl_flash_processor.py`<br>`qwen3_vl_plus_processor.py`<br>`qwen35_flash_processor.py`<br>`qwen35_plus_processor.py`<br>`openrouter_processor.py` | 调用不同视觉大模型 API 进行视频内容分析 |
| **抽帧逻辑** | 各 processor 文件 | 通过 API `fps` 参数控制（默认 1.0 fps），后端完成抽帧 |
| **动态上下文构建** | 与 `context/` 模块协作 | 加载当天事件缓存、人物外貌表缓存、二维码识别结果 |
| **提示词构建** | `context/prompt_builder.py` | 构建包含动态上下文的模型提示词 |
| **结果解析** | 各 processor 文件 | 解析模型输出的续写事件和外貌更新 |

### 2.4 后端 - 上下文管理 (`context/`)

| 功能 | 文件 | 说明 |
|-----|------|------|
| **人物外貌缓存** | `appearance_cache.py` | 维护人物外貌表，支持 add/update/merge 操作，使用并查集管理编号合并 |
| **事件上下文** | `event_context.py` | 维护当天事件缓存（最新 n 条），从 JSONL 文件加载 |
| **提示词构建** | `prompt_builder.py` | 整合事件、外貌、二维码信息构建模型提示词 |

### 2.5 后端 - 数据存储 (`storage/`)

| 功能 | 文件 | 说明 |
|-----|------|------|
| **SeekDB 客户端** | `seekdb_client.py` | 数据库连接与操作封装 |
| **数据模型** | `models.py` | EventLog、Appearance 等数据模型定义 |
| **数据库 Schema** | `schema.sql` | 表结构定义（用户、事件、外貌、索引等） |

### 2.6 后端 - 日志写入 (`log_writer/`)

| 功能 | 文件 | 说明 |
|-----|------|------|
| **日志写入** | `writer.py` | 事件写入数据库，支持调试 JSONL 输出 |
| **加密服务** | `encryption_service.py` | 字段级加密（AES-GCM + RSA-OAEP），日终触发 |

### 2.7 后端 - 视频分段 (`segmentation/`)

| 功能 | 文件 | 说明 |
|-----|------|------|
| **关键帧分段** | `segmenter.py` | 关键帧对齐，迭代式分段（约 60 秒/段），确保连续性 |

### 2.8 后端 - 向量索引 (`indexing/`)

| 功能 | 文件 | 说明 |
|-----|------|------|
| **文本分块** | `chunker.py` | 日志内容分块策略 |
| **分块策略** | `chunking_strategies.py` | 支持多种分块策略（按事件、按时间等） |
| **嵌入服务** | `embedding_service.py` | 使用 Qwen text-embedding-v4 生成 1024 维向量 |

### 2.9 后端 - 流程编排 (`orchestration/`)

| 功能 | 文件 | 说明 |
|-----|------|------|
| **处理流水线** | `pipeline.py` | 视频处理流水线编排，协调各模块工作 |

### 2.10 后端 - Web API (`web_api/`)

| 功能 | 文件 | 说明 |
|-----|------|------|
| **用户认证** | `routers/auth.py` | 注册、登录、JWT Token |
| **用户管理** | `routers/users.py` | 用户信息查询、修改 |
| **管理员接口** | `routers/admin.py` | 数据库查询、用户管理 |
| **紧急事件** | `routers/emergencies.py` | 紧急事件查询、实时提醒接口 |
| **主应用** | `main.py` | FastAPI 应用入口，CORS 配置 |

### 2.11 Web 前端 (`web_ui/`)

| 功能 | 路径 | 说明 |
|-----|------|------|
| **用户界面** | `src/App.jsx` | 主应用组件 |
| **API 调用** | `src/api/` | 后端 API 封装 |
| **UI 组件** | `src/components/` | 可复用组件 |
| **自定义 Hooks** | `src/hooks/` | 状态管理 Hooks |
| **工具函数** | `src/utils/` | 通用工具 |

### 2.12 配置与基础设施

| 模块 | 文件/目录 | 说明 |
|-----|-----------|------|
| **数据库配置** | `config/database_config.py` | 数据库连接配置 |
| **加密配置** | `config/encryption_config.py` | 加密密钥配置 |
| **Nginx 配置** | `nginx/lab-log.conf` | 反向代理配置 |
| **工具脚本** | `utils/` | 时间解析等通用工具 |

---

## 三、核心业务流程

### 3.1 实时处理流程（Android → 后端）

```
Android 采集端：
Camera2 API → YUV_420_888 → NV12 转换 → 时间戳水印绘制
→ H.264/H.265 编码 → MediaMuxer 封装 MP4 分段
→ WebSocket 发送（含二维码识别结果）

后端流媒体服务器：
WebSocket 接收 → 保存 MP4 分段 + QR JSON 元数据
→ [可选] 实时视频理解 → 写入数据库
```

### 3.2 视频理解流程（动态上下文模式）

```
输入：MP4 视频文件 + 二维码识别结果
  ↓
[视频分段] 关键帧对齐，约 60 秒/段（迭代式确保连续性）
  ↓
[动态上下文构建]
  ├─ 查询当天最新 n 条事件（event_logs.jsonl）
  ├─ 加载人物外貌表缓存（appearances_today.json）
  └─ 获取二维码识别结果
  ↓
[视频理解] 调用 Qwen3-VL / Qwen3.5 API
  ├─ 输入：动态上下文 + 视频片段
  └─ 输出：events_to_append（续写事件）、appearance_updates（外貌更新）
  ↓
[事件写入] 立即写入 SeekDB（不加密）+ JSONL 调试文件
  ↓
[外貌更新] 更新内存中的外貌表缓存
  ↓
[周期性保存] 每 N 个分段 dump 外貌缓存到文件
  ↓
[日终处理] 并查集压缩 → 公钥获取与加密 → 写入数据库 → 更新 person_ids → 触发索引
```

### 3.3 日终处理流程

```
触发条件：每日结束或手动触发
  ↓
[并查集压缩] 合并重复人物编号
  ↓
[动态公钥获取] 根据识别出的用户身份获取对应公钥
  ↓
[字段加密] 敏感字段（外貌特征）使用 AES-GCM + RSA-OAEP 加密
  ↓
[数据入库] 加密数据写入数据库
  ↓
[更新事件] 更新事件表中的 person_ids 关联
  ↓
[触发索引] 构建向量索引，支持语义搜索
```

---

## 四、关键功能特性

### 4.1 已实现的特性

| 特性 | 状态 | 说明 |
|-----|------|------|
| 动态上下文视频理解 | ✅ | 基于当天事件和外貌缓存构建提示词，模型续写事件 |
| 多模型支持 | ✅ | Qwen3-VL、Qwen3.5-Flash/Plus、Gemini2.5、OpenRouter |
| 人物外貌管理 | ✅ | 自动维护外貌表，支持追加/更新/合并，并查集管理 |
| 二维码识别 | ✅ | Android 端实时识别，按分段聚合，随视频上报 |
| 字段级加密 | ✅ | 敏感字段混合加密（AES-GCM + RSA-OAEP），日终触发 |
| 向量检索 | ✅ | 日志向量化，支持语义搜索 |
| 紧急事件提醒 | ✅ | 视觉模型识别紧急情况，Web 弹窗/变红提醒 |
| 权限管理 | ✅ | admin/user 角色区分，admin 可查看数据库和向量搜索 |
| H.265/H.264 自动回退 | ✅ | 优先 H.265，不支持时自动降级 H.264 |
| 时间戳水印 | ✅ | OCR-B 字体，NV12 Y平面绘制，客户端本地时间 |

### 4.2 技术亮点

- **关键帧对齐分段**：确保视频片段边界在关键帧处，避免解码问题
- **迭代式分段**：智能调整分段点，保证视频连续性
- **SPS/PPS 前置**：每个 IDR 帧前附加完整 SPS/PPS，保证每段 MP4 可独立解码
- **动态公钥加密**：日终根据识别用户自动获取对应公钥加密，确保隐私数据仅用户可解
- **并查集人物管理**：高效处理人物编号合并关系

---

## 五、脚本工具 (`scripts/`)

| 脚本 | 功能 |
|------|------|
| `init_database.py` | 数据库初始化 |
| `end_of_day.py` | 日终处理（加密、索引） |
| `process_recording_session.py` | 处理录制会话（离线处理） |
| `process_video.py` | 单个视频处理 |
| `index_events.py` | 事件向量索引 |
| `test_vector_search.py` | 向量搜索测试 |
| `test_segmentation.py` | 视频分段测试 |
| `analyze_keyframes.py` | 关键帧分析 |
| `concat_videos.py` | 视频拼接 |
| `clear_test_data.py` | 清理测试数据 |
| `export_users.py` | 导出用户数据 |
| `check_user.py` | 检查用户信息 |
| `migrate_users_table.py` | 用户表迁移 |

---

## 六、目录结构速览

```
lab-log/
├── android-app-trial/      # Android 客户端（试用版）
├── android-camera/         # Android 客户端（旧版）
├── config/                 # 配置文件
├── context/                # 上下文管理（事件、外貌缓存）
├── indexing/               # 向量索引
├── log_writer/             # 日志写入
├── nginx/                  # Nginx 配置
├── orchestration/          # 流程编排
├── scripts/                # 工具脚本
├── segmentation/           # 视频分段
├── storage/                # 数据存储（SeekDB）
├── streaming_server/       # WebSocket 流媒体服务器
├── utils/                  # 通用工具
├── video_processing/       # 视频处理（视觉大模型调用）
├── web_api/                # FastAPI Web 接口
├── web_ui/                 # React 前端
├── archive/                # 历史归档
└── 项目功能点梳理.md        # 本文档
```

---

## 七、缺陷与改善思路

### 7.1 时间戳来源问题

#### 问题描述

当前系统的时间戳生成逻辑位于 Android 客户端，使用设备本地时间（`System.currentTimeMillis()`）作为水印绘制和分段文件名的依据。这意味着：

1. 水印视频中显示的时间完全取决于客户端设备的本地时钟
2. 后端接收到的分段文件名中的时间戳也来自客户端本地时间
3. 如果客户端修改系统时间、出现时钟漂移，或处于不同时区，最终记录的时间将与实际发生时间产生偏差

这对于实验室场景是一个严重隐患——日志的时间准确性是核心需求，一旦时间错乱，后续的事件追溯和审计都将失去意义。

#### 问题分析

**根本原因**：
- 当前架构采用"客户端生成、后端接收"模式，时间戳的权威性完全下放给客户端
- 缺乏中心化的时钟同步机制

**风险场景**：
| 场景 | 影响 |
|------|------|
| 用户手动修改系统时间 | 录像时间被恶意或无意篡改 |
| 设备长期未同步 NTP | 时钟漂移导致时间逐渐偏离 |
| 跨时区使用 | 不同设备时间基准不一致 |
| 设备重启后时间重置 | 时间回退到过去某个时间点 |

**现有代码位置**：
- `android-app-trial/app/src/main/java/com/example/lablog/utils/OcrBFontRenderer.kt`：时间戳水印绘制
- `android-app-trial/app/src/main/java/com/example/lablog/viewmodel/RecordingViewModel.kt`：分段文件名时间戳生成

#### 解决方案

**方案一：客户端通过 NTP 从后端获取时间（推荐）**

实现思路：
1. 在后端服务器部署 NTP 服务（如 `ntpd` 或 `chronyd`），确保服务器时间通过 NTP 与世界时钟同步
2. 后端开放 NTP 端口供内网客户端访问
3. Android 客户端引入 NTP 客户端库（如 [commons-net](https://commons.apache.org/proper/commons-net/) 的 `NTPUDPClient`）
4. 在录像启动或分段切换时，客户端向服务器请求当前时间，以此作为水印和文件名的依据

优点：
- 改动范围相对集中，仅需修改客户端时间获取逻辑
- 使用标准 NTP 协议，可靠性高
- 后端 NTP 服务可自行配置上游时钟源，保证权威性

缺点：
- 每次获取时间需要一次网络请求（约几十毫秒），对实时水印绘制有一定延迟
- 如果 NTP 请求失败，需要有降级策略（如缓存上次偏移量）

**方案二：客户端通过 HTTP 从后端获取时间**

实现思路：
1. 后端维护一个 `/time` 接口，返回当前服务器时间戳
2. 后端服务器仍需通过 NTP 服务同步自身时钟
3. 客户端启动时或定时向后端请求时间，计算本地时间与服务器时间的偏移量（`offset = serverTime - localTime`）
4. 后续本地生成时间戳时，自动应用该偏移量：`correctedTime = localTime + offset`

优点：
- 复用现有的 HTTP/WebSocket 通道，无需开放额外端口
- 可实现更复杂的业务逻辑（如身份验证后再提供时间）
- 偏移量计算后，后续时间生成为本地操作，无网络延迟

缺点：
- 需要定期重新校准偏移量（应对客户端时钟漂移）
- 首次启动或长周期运行后需要重新同步

**方案三：Python 客户端的重构方案**

如果后续客户端改用 Python 实现，时间戳逻辑需要完全重写，建议采用以下架构：

```python
# 伪代码示例：Python 客户端时间同步模块
import time
import requests
from datetime import datetime, timedelta

class TimeSyncManager:
    def __init__(self, backend_url):
        self.backend_url = backend_url
        self.offset = 0
        self.last_sync = 0
        self.sync_interval = 300  # 每5分钟重新校准

    def sync_time(self):
        """与后端同步时间，计算偏移量"""
        # 可采用 HTTP 或 NTP 方式
        server_time = self._fetch_server_time()
        local_time = time.time() * 1000
        self.offset = server_time - local_time
        self.last_sync = time.time()

    def get_corrected_time(self):
        """获取修正后的时间戳"""
        if time.time() - self.last_sync > self.sync_interval:
            self.sync_time()  # 定期重新同步
        return int(time.time() * 1000 + self.offset)

    def _fetch_server_time(self) -> int:
        # 实现：HTTP 请求或 NTP 查询
        pass
```

对于 Python 客户端，推荐优先考虑 `ntplib` 库实现 NTP 客户端，或 `requests` 库实现 HTTP 时间同步。

**实施建议**：

| 优先级 | 方案 | 适用场景 |
|--------|------|----------|
| 首选 | 方案二（HTTP 时间同步） | 当前 Kotlin 客户端改造，改动小，复用现有通道 |
| 备选 | 方案一（NTP 协议） | 对时间精度要求极高，或后续独立部署专用采集设备 |
| 规划 | 方案三（Python 重构） | 客户端技术栈迁移时同步实施 |

**需要关注的技术点**：
1. 网络延迟补偿：无论是 NTP 还是 HTTP，都应考虑往返时间（RTT）的一半作为延迟补偿
2. 失败降级：同步失败时，记录日志并继续使用本地时间（带偏移量缓存）
3. 水印显示：水印中可同时显示"同步状态"标识（如小圆点颜色），提示用户时间是否已同步

---

### 7.2 后端视频理解的数据推送与上下文连续性机制

#### 问题描述

在讨论视频推送实时性之前，需要先理解后端如何将视频数据推送给视觉大模型进行解读，以及代码如何保证**跨分段的上下文连续性**。

#### 数据推送机制

**整体流程**：

```
视频分段 → 构建动态提示词 → 调用视觉大模型 API → 解析响应 → 更新上下文
```

**关键代码位置**：
- 入口：`video_processing/qwen3_vl_flash_processor.py:238-324` (`process_segment_with_context`)
- 提示词构建：`context/prompt_builder.py:23-166` (`build_dynamic_prompt`)
- 事件上下文管理：`context/event_context.py`
- 人物外貌缓存：`context/appearance_cache.py`

**具体推送过程**：

1. **视频文件引用**：后端并不直接将视频字节流发送给大模型，而是构造 `file://` 协议的本地文件路径
   ```python
   video_url = f"file://{video_path}"
   ```

2. **构建多模态消息**：使用 DashScope SDK 的 `MultiModalConversation.call()`
   ```python
   messages = [
       {
           'role': 'system',
           'content': self.prompt_builder.build_system_instruction()
       },
       {
           'role': 'user',
           'content': [
               {'video': video_url, 'fps': self.fps},  # 视频 + 抽帧率
               {'text': prompt}  # 动态构建的提示词
           ]
       }
   ]
   ```

3. **API 调用**：由 DashScope SDK 负责读取本地视频文件并上传到阿里云进行视觉理解
   ```python
   response = MultiModalConversation.call(
       api_key=self.api_key,
       model=self.model,  # 如 qwen3-vl-flash
       messages=messages,
       fps=self.fps,  # 控制抽帧率，默认 1.0 fps
       ...
   )
   ```

#### 上下文连续性保证机制

**核心问题**：由于视频被切分为多个分段（约 60 秒/段），如何保证模型能识别出**同一人物在不同分段中的连续性**？

**解决方案**：通过**动态上下文提示**将历史信息注入每个分段的分析中。

**1. 事件上下文（EventContext）**

文件：`context/event_context.py`

功能：
- 从 JSONL 文件 (`logs_debug/event_logs.jsonl`) 读取当天已发生的事件
- 提供 `get_recent_events(n=20)` 获取最近 N 条事件
- 提供 `get_max_event_id_number()` 获取当前最大事件编号，用于新事件编号递增

连续性保证：
- 每个分段处理时，将最近事件（默认 20 条）格式化为文本加入提示词
- 模型基于这些事件了解"之前发生了什么"
- 新事件编号从 `max_event_id + 1` 开始，保证 ID 连续性

**2. 人物外貌缓存（AppearanceCache）**

文件：`context/appearance_cache.py`

核心机制：

| 机制 | 说明 |
|------|------|
| 外貌表 | 维护 `person_id → 外貌描述` 的映射 |
| 全量提示 | 每次将完整外貌表（而不仅是最近记录）加入提示词 |
| 并查集合并 | 使用 UnionFind 管理人物编号合并关系（如 p1 和 p3 被识别为同一人） |
| 编号递增 | 新人物编号必须 > 现存最大编号 |

关键代码（`appearance_cache.py:183-213`）：
```python
def get_for_prompt(self) -> Tuple[List[Dict], List[Dict]]:
    """获取用于提示词的外貌表数据（剥离日期后缀）"""
    roots = self.union_find.get_roots()
    main_records = []

    for root in roots:
        if root in self.records:
            record = self.records[root]
            main_records.append({
                "person_id": self._strip_date(record.person_id),
                "appearance": record.appearance,
                "user_id": record.user_id
            })

    return main_records, all_aliases
```

**3. 动态提示词构建（PromptBuilder）**

文件：`context/prompt_builder.py`

提示词结构：
```
## 二维码识别结果
（本视频片段的二维码识别结果）

## 最近事件记录（参考描述风格，共 X 条）
- evt_00001 | 2025-12-24T10:00:00 ~ 2025-12-24T10:00:20 | person | 人物: p1 | ...

## 人物外貌表（已全量给出）
### 人物记录
- p1: 短黑发，戴黑框眼镜，穿白色实验服...
- p2: 金色长发，穿蓝色实验服...

### 编号别名（已合并）
- p3 -> p1

## 任务要求
1. 优先重用已有外貌记录匹配当前画面中的人物
2. 发现全新人物时，从 p{next_person_id} 开始递增
3. 新事件编号从 evt_{next_event_id:05d} 开始递增
...
```

**模型输出格式**：
```json
{
  "events_to_append": [...],  // 续写事件
  "appearance_updates": [...], // 外貌表更新（add/update/merge）
  "emergency_events": [...]    // 紧急情况
}
```

**4. 上下文更新流程**

```
处理分段 N:
  ├─ 加载外貌缓存（内存中）
  ├─ 加载最近事件（从 JSONL 文件）
  ├─ 构建提示词 → 调用大模型
  ├─ 解析响应得到 events_to_append 和 appearance_updates
  ├─ 立即写入数据库 + JSONL（不加密）
  ├─ 应用外貌更新到内存缓存
  └─ 周期性保存外貌缓存到文件（每 N 个分段）
```

**关键连续性保障点**：

| 问题 | 解决方案 |
|------|----------|
| 人物跨分段识别 | 外貌表全量提示，模型基于描述匹配 |
| 事件编号连续 | 从 JSONL 实时查询 max_event_id |
| 人物编号连续 | 缓存中维护 max_person_id，新编号必须递增 |
| 人物合并 | 并查集管理合并关系，merge 操作记录到缓存 |
| 服务重启恢复 | 从文件加载外貌缓存和 JSONL 事件 |

**存在的问题**：

1. **事件提示有限**：仅提示最近 20 条事件，如果分段跨度大，早期事件可能被遗漏
2. **外貌表膨胀**：随着时间推移，外貌表会越来越大，提示词长度增加
3. **文件 IO 频繁**：每个分段都需读取 JSONL 文件查询事件，高并发时可能成为瓶颈
4. **无事务保证**：分段处理和上下文更新非原子操作，崩溃可能导致不一致

**与实时推送的关系**：

当前的分段级上下文机制**不依赖于实时推送**——无论分段是 60 秒还是 5 秒，上下文更新逻辑都是一样的。

如果改为真正的实时流式推送，上下文机制需要调整：
- 当前基于"分段 → 完整处理 → 更新上下文"的批处理模式
- 实时流式需要改为"定时抽帧 → 增量分析 → 即时更新上下文"的模式
- 外貌表和事件缓存需要支持更细粒度的增量更新

---

### 7.3 视频推送实时性问题

#### 问题描述

当前系统采用**分段准实时**推送机制：

1. **客户端录制**：Android 端采集视频并本地编码为 H.264/H.265
2. **本地分段**：使用 `MP4SegmentMuxer` 将视频按时间切片（默认约 60 秒/段）
3. **分段推送**：每完成一段 MP4 封装后，通过 WebSocket 以 Base64 编码形式发送到后端
4. **后端处理**：后端接收完整分段后保存，并加入队列进行视频理解

**关键代码位置**：
- 客户端分段逻辑：`android-app-trial/app/src/main/java/com/example/lablogcamera/OriginalMainActivity.kt:402-418`
- 客户端推送逻辑：`android-app-trial/app/src/main/java/com/example/lablogcamera/OriginalMainActivity.kt:1888-1977`
- 后端接收逻辑：`streaming_server/server.py:565-630`

#### 问题分析

**当前方案的实时性评估**：

| 指标 | 当前表现 | 说明 |
|------|----------|------|
| 端到端延迟 | 60~120 秒 | 需等待分段完成 + 网络传输 + 处理时间 |
| 传输方式 | 准实时 | 分段级传输，非帧级流式 |
| 数据格式 | MP4 文件 | 完整文件封装后传输 |

**是否算"实时"？**

严格来说，**当前不是实时推送**，而是"准实时"或"准实时批量传输"：
- 不是 RTMP/WebRTC 那样的流式传输
- 延迟至少有一个分段时长（60秒）
- 对紧急事件的检测存在固有延迟

**当前方案的优势**：
1. **可靠性高**：MP4 是完整容器，包含索引，容错性强
2. **便于处理**：后端可直接用 FFmpeg 等工具处理完整文件
3. **网络友好**：批量传输比逐帧传输开销小
4. **实现简单**：复用现有 WebSocket 通道，无需额外协议

**当前方案的劣势**：
1. **延迟高**：60秒以上的事件检测延迟
2. **内存压力**：客户端需要缓存整段视频（约几MB到几十MB）
3. **失效风险**：网络中断会导致当前分段全部丢失

#### 解决方案

**方案一：保持当前准实时方案（推荐当前阶段）**

适用场景：
- 实验室环境网络稳定
- 对实时性要求不极端（能接受分钟级延迟）
- 需要快速迭代 MVP

优化方向：
- 缩短分段时长（如从 60 秒降至 10~20 秒）
- 优化视频理解处理速度
- 添加分段级确认机制（ACK）保证不丢段

**方案二：改为真正的实时流式推送**

实现思路：
1. **协议升级**：改用 RTMP、RTSP 或 WebRTC 进行流传输
2. **后端改造**：使用支持流式输入的媒体服务器（如 SRS、MediaMTX）
3. **处理逻辑调整**：
   - 后端实时接收流，同时写入文件（支持回看）
   - 定时（如每 5 秒）对流进行截图或抽帧
   - 将抽帧结果送入视频理解模块

优点：
- 真正的低延迟（秒级甚至亚秒级）
- 紧急事件可立即触发告警
- 符合传统视频监控的实时性预期

缺点：
- **架构复杂度高**：需引入流媒体服务器，增加部署成本
- **与当前代码不兼容**：需重写客户端编码推送逻辑、后端接收逻辑
- **处理逻辑需调整**：当前视频理解基于完整 MP4 文件，改为流式后需重新设计抽帧和上下文维护策略
- **资源消耗增加**：持续的网络连接和实时处理对服务器压力更大

**方案三：折中方案——短分段 + 重叠窗口**

实现思路：
1. 保持现有 MP4 分段架构
2. 大幅缩短分段时长（如 5~10 秒）
3. 分段之间设置重叠窗口（如重叠 2 秒），避免事件被切分在边界
4. 后端并行处理多个短分段

优点：
- 兼容现有架构，改动量可控
- 延迟降低一个数量级
- 保持 MP4 的可靠性优势

缺点：
- 分段数增加，处理开销增大
- 重叠窗口带来重复计算
- 网络开销略有增加

#### 技术可行性评估

**如果改为实时推送，当前方案是否可用？**

| 组件 | 可用性 | 说明 |
|------|--------|------|
| 客户端编码器 | ✅ 大部分可用 | `VideoEncoder` 编码逻辑可复用，但需改为流式输出 |
| 客户端网络层 | ❌ 需重写 | WebSocket 发送 MP4 文件需改为流式协议（RTMP/WebRTC） |
| 后端接收服务 | ❌ 需重写 | `streaming_server/server.py` 基于分段文件接收，无法直接处理流 |
| 视频理解模块 | ⚠️ 需调整 | 当前基于完整 MP4 文件，改为流式后需改为定时抽帧或流式输入 |
| 上下文管理 | ✅ 可用 | `AppearanceCache`、`EventContext` 等逻辑与传输方式无关 |

**结论**：

如果决定改为真正的实时推送，**当前方案基本无法直接复用**，相当于需要重构整个传输层：
1. 客户端：从"分段编码→文件→WebSocket"改为"持续编码→流式协议推送"
2. 后端：从"WebSocket 接收文件→保存→处理"改为"流服务器接收→实时抽帧→处理"

**建议决策路径**：

```
是否需要秒级事件检测？
├── 是 → 能否接受架构重构成本？
│       ├── 能 → 方案二（实时流式推送）
│       └── 不能 → 方案三（短分段+重叠）
└── 否 → 方案一（保持当前，可缩短分段时长）
```

当前阶段建议采用**方案一（保持准实时，优化分段时长）**或**方案三（短分段+重叠）**，待产品验证后再考虑是否投入资源进行实时流式改造。

---

### 7.4 预研方向：引入 YOLO 进行静态场景识别

#### 背景与动机

当前系统完全依赖视觉大模型（如 Qwen3-VL）进行视频理解，存在以下痛点：

1. **资源消耗高**：大模型 API 调用成本高，且每次调用需要处理完整分段
2. **实时性受限**：最小分段时长受限于处理开销，无法做到秒级响应
3. **静态场景浪费**：实验室无人物活动时，仍在持续推流和处理
4. **告警延迟**：紧急情况检测依赖大模型推理，存在分钟级延迟

**YOLO（You Only Look Once）等轻量级目标检测模型的优势**：
- 在移动设备可实时运行（30+ FPS）
- 功耗和计算成本远低于大模型 API 调用
- 适合处理明确的、规则化的视觉任务

---

#### 方向一：客户端集成 YOLO，无人时不推流（高优先级）

**实现思路**：

在 Android 客户端引入 YOLO 模型（如 YOLOv8n 或更轻量的 YOLOv5n），进行实时人物检测：

```
采集视频流
    ↓
YOLO 人物检测（本地运行，10-30 FPS）
    ↓
检测结果判断
    ├─ 连续 N 帧无人 → 暂停推流，进入"休眠模式"
    └─ 检测到人物 → 开始/恢复推流
```

**技术方案**：

| 方案 | 模型 | 推理框架 | 特点 |
|------|------|----------|------|
| A | YOLOv8n | NCNN/MNN | 轻量，适合中低端设备 |
| B | YOLO11n | TensorFlow Lite | 最新版本，精度更高 |
| C | MobileNet-SSD | TFLite | 更轻量，但精度较低 |

**推荐方案 A**：
- 使用 YOLOv8n（Nano 版本，约 3.2MB）
- NCNN 框架针对 ARM 优化，在 Android 上性能优异
- 只检测"person"一类目标，减少计算量

**休眠/唤醒策略**：

```kotlin
// 伪代码示例
class YoloPresenceDetector {
    private var consecutiveEmptyFrames = 0
    private var consecutivePersonFrames = 0
    private val EMPTY_THRESHOLD = 30  // 约 1 秒（30 FPS）
    private val PERSON_THRESHOLD = 5  // 约 0.16 秒

    fun processFrame(bitmap: Bitmap): PresenceState {
        val persons = yolo.detect(bitmap, filterClass = "person")

        return if (persons.isEmpty()) {
            consecutiveEmptyFrames++
            consecutivePersonFrames = 0

            if (consecutiveEmptyFrames >= EMPTY_THRESHOLD) {
                PresenceState.ABSENT  // 触发休眠
            } else {
                PresenceState.TRANSITIONING
            }
        } else {
            consecutivePersonFrames++
            consecutiveEmptyFrames = 0

            if (consecutivePersonFrames >= PERSON_THRESHOLD) {
                PresenceState.PRESENT  // 触发唤醒
            } else {
                PresenceState.TRANSITIONING
            }
        }
    }
}
```

**预期收益**：

| 指标 | 改善前 | 改善后 | 收益 |
|------|--------|--------|------|
| 无人时推流流量 | 100% | ~5%（心跳包） | 节省 95% 无效流量 |
| 大模型 API 调用 | 24 小时/天 | 仅有人时调用 | 节省 70-90% API 费用 |
| 设备发热 | 高 | 低 | 延长设备寿命 |
| 电池续航 | 4-6 小时 | 8-12 小时 | 支持全天使用 |

**潜在问题与解决方案**：

| 问题 | 解决方案 |
|------|----------|
| YOLO 模型体积增大 APK | 使用动态下载，首次启动时从服务器下载模型文件 |
| 检测精度不足 | 实验室场景相对固定，可针对环境 fine-tune |
| 人物被遮挡漏检 | 设置保守的唤醒阈值，宁可误报也不漏报 |
| 延迟唤醒导致事件丢失 | 保留 5 秒预录制缓冲区，唤醒时补发 |

**实施建议**：

这是一个**高 ROI** 的改进，建议优先实施：
1. 第一阶段：集成 YOLO，实现基础的人物存在检测
2. 第二阶段：优化休眠/唤醒策略，根据实际场景调参
3. 第三阶段：考虑结合动作检测（检测是否有"有效动作"而非仅仅是存在）

---

#### 方向二：YOLO 静态告警检测（探索性讨论）

**思路**：对于一些**规则明确、视觉特征固定**的告警场景，使用 YOLO 在客户端本地检测，通过独立接口上报后端生成系统警告日志。

**候选告警场景**：

| 场景 | 视觉特征 | YOLO 适用性 | 优先级 |
|------|----------|-------------|--------|
| 消防通道堵塞 | 特定区域出现箱子/设备 | ⭐⭐⭐⭐ 高 | P0 |
| 化学品未放回指定架 | 试剂瓶出现在实验台面 | ⭐⭐⭐ 中高 | P1 |
| 设备运行状态灯异常 | LED 颜色/闪烁模式 | ⭐⭐ 中 | P2 |
| 防护装备未佩戴 | 人员头部/手部特征 | ⭐⭐ 中（可能被遮挡） | P2 |
| 明火/烟雾 | 颜色+形态特征 | ⭐⭐ 中（大模型已覆盖） | P3 |

**架构设计**：

```
┌─────────────────────────────────────────────────────────────┐
│                        Android 客户端                        │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │   视频采集    │───▶│  YOLO 检测    │    │  推流控制    │  │
│  └──────────────┘    └──────┬───────┘    └──────────────┘  │
│                             │                               │
│                    ┌────────┴────────┐                     │
│                    ▼                 ▼                     │
│            ┌──────────────┐  ┌──────────────┐             │
│            │ 人物检测      │  │ 静态告警检测  │             │
│            │ (方向一)      │  │ (方向二)      │             │
│            └──────┬───────┘  └──────┬───────┘             │
│                   │                  │                     │
│                   ▼                  ▼                     │
│            唤醒/休眠推流      HTTP 上报告警                 │
│                                    │                       │
└────────────────────────────────────┼───────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────┐
│                           后端服务                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  /alerts/static 接口（新增）                          │   │
│  │  - 接收 YOLO 检测到的静态告警                          │   │
│  │  - 直接写入数据库（alert_logs 表）                     │   │
│  │  - 触发 WebSocket/推送通知                            │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  /emergencies 接口（现有）                            │   │
│  │  - 接收大模型检测的紧急情况（如火焰）                   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**接口设计（提案）**：

```python
# 新增静态告警上报接口
@router.post("/alerts/static")
def report_static_alert(
    alert_type: str,        # 告警类型：blocked_aisle, chemical_misplaced 等
    confidence: float,      # YOLO 置信度
    snapshot: bytes,        # Base64 编码的告警截图
    camera_id: str,         # 摄像头标识
    timestamp: datetime,    # 检测时间戳
    location_hint: Optional[str] = None  # 位置提示（如"实验区A"）
):
    """
    客户端 YOLO 检测到静态违规时调用
    直接生成系统警告日志，不经过大模型
    """
    pass
```

**优势分析**：

1. **即时响应**：YOLO 检测延迟 < 100ms，告警可秒级上报
2. **降低成本**：无需调用大模型 API 即可发现静态违规
3. **隐私友好**：敏感画面处理在本地完成，仅上报告警摘要和截图
4. **离线可用**：即使网络中断，也可本地缓存告警稍后上报

**劣势与风险**：

| 风险 | 说明 | 缓解措施 |
|------|------|----------|
| **误报率高** | YOLO 无法理解"临时放置"vs"违规摆放"的区别 | 设置置信度阈值，结合时间维度（持续 N 分钟才告警） |
| **维护成本高** | 实验室布局变化需重新训练模型 | 使用可配置的 ROI（感兴趣区域），而非端到端训练 |
| **覆盖场景有限** | 复杂语义场景（如"操作不规范"）无法检测 | 明确 YOLO 边界，复杂场景仍用大模型 |
| **多端一致性** | 不同设备检测结果可能不一致 | 后端设置防重复机制，同一告警 N 分钟内只记录一次 |

**可行性评估**：

| 场景 | 推荐方案 | 理由 |
|------|----------|------|
| 消防通道堵塞检测 | **推荐实施** | ROI 固定，规则明确，YOLO 检测物体存在即可 |
| 化学品归位检测 | **有条件推荐** | 需配合实验室物资管理系统，知道"应该在哪里" |
| 设备状态灯监控 | **待定** | 可能需要图像分类而非目标检测，考虑传统 CV |
| 防护装备检测 | **不推荐** | 遮挡严重，误报率高，建议仍用大模型 |

**建议的演进路径**：

```
Phase 1（当前）: 仅大模型检测紧急情况（已实现）
        ↓
Phase 2（短期）: 方向一 + 最简单的静态检测（消防通道）
        ↓
Phase 3（中期）: 评估 Phase 2 效果，决定是否扩展更多静态场景
        ↓
Phase 4（长期）: 大小模型协同——YOLO 负责实时初筛，大模型负责复杂语义理解
```

**结论**：

- **方向一（无人检测）**：强烈建议实施，ROI 明确，技术成熟
- **方向二（静态告警）**：建议从单一、明确的场景（如消防通道堵塞）开始试点，验证效果后再扩展。避免过早引入过多规则导致系统复杂化。

---

### 7.5 代码测试现状与改进方案

#### 现状盘点

**后端（Python）测试情况**：

| 类别 | 现状 | 位置 |
|------|------|------|
| 单元测试 | ❌ 无 | - |
| 集成测试 | ❌ 无 | - |
| 功能测试脚本 | ⚠️ 3 个 | `scripts/test_segmentation.py`、`scripts/test_vector_search.py`、`streaming_server/test_qr_server.py` |
| 测试框架配置 | ❌ 无 | 无 `pytest.ini`、`setup.cfg` 或 `pyproject.toml` 测试配置 |
| 覆盖率工具 | ❌ 无 | 无 `coverage.py` 配置 |

**功能测试脚本分析**：

1. `scripts/test_segmentation.py`：命令行工具，测试视频分段逻辑，需要传入真实视频文件
2. `scripts/test_vector_search.py`：测试向量搜索功能，依赖真实数据库连接和 Embedding 服务
3. `streaming_server/test_qr_server.py`：二维码测试服务器，用于手动测试客户端推送

**前端（Android）测试情况**：

| 类别 | 现状 | 位置 |
|------|------|------|
| 单元测试 | ⚠️ 仅示例 | `ExampleUnitTest.kt`（只有 `2+2=4` 的占位测试） |
| 集成测试 | ❌ 无 | - |
| UI 测试 | ❌ 无 | 无 `androidTest` 目录 |

**问题总结**：

1. **零单元测试覆盖**：核心业务逻辑（如视频处理、上下文管理、外貌缓存）完全没有自动化测试
2. **测试脚本即工具**：现有的 `test_*.py` 实际上是手动调试工具，而非自动化测试
3. **依赖外部服务**：测试脚本依赖真实的数据库、API 服务，无法在隔离环境运行
4. **无 CI/CD 流程**：没有 GitHub Actions 或其他持续集成配置
5. **无回归测试保障**：代码重构风险高，容易引入回归缺陷

#### 测试引入方案

**阶段一：基础设施搭建（1-2 天）**

**1. Python 后端测试框架**

```bash
# 安装测试依赖
pip install pytest pytest-asyncio pytest-mock pytest-cov httpx
```

`pyproject.toml` 测试配置：
```toml
[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-mock>=3.10.0",
    "pytest-cov>=4.0.0",
    "httpx>=0.25.0",  # 用于 FastAPI 异步测试
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --cov=. --cov-report=term-missing --cov-report=html"
asyncio_mode = "auto"
```

**2. 测试目录结构**

```
lab-log/
├── tests/
│   ├── __init__.py
│   ├── conftest.py              # pytest 共享 fixtures
│   ├── unit/                    # 单元测试
│   │   ├── context/
│   │   │   ├── test_appearance_cache.py
│   │   │   ├── test_event_context.py
│   │   │   └── test_prompt_builder.py
│   │   ├── video_processing/
│   │   │   └── test_processor_response_parser.py
│   │   ├── storage/
│   │   │   └── test_models.py
│   │   └── utils/
│   │       └── test_segment_time_parser.py
│   ├── integration/             # 集成测试
│   │   ├── test_streaming_server.py
│   │   └── test_end_to_end.py
│   └── fixtures/                # 测试数据
│       ├── sample_segment.json
│       └── sample_video.mp4
└── pytest.ini
```

**阶段二：核心模块单元测试（优先级排序）**

**P0 - 最高优先级（业务核心）**：

```python
# tests/unit/context/test_appearance_cache.py
import pytest
from context.appearance_cache import AppearanceCache, AppearanceRecord

class TestAppearanceCache:
    """人物外貌缓存测试"""

    @pytest.fixture
    def cache(self):
        cache = AppearanceCache()
        cache.nominal_date = "2025-12-24"
        return cache

    def test_add_new_person(self, cache):
        """测试添加新人物"""
        cache.add("p1", "短黑发，戴眼镜", user_id="user_001")
        record = cache.get_record("p1")
        assert record is not None
        assert record.person_id == "p1_2025-12-24"
        assert record.user_id == "user_001"

    def test_add_duplicate_raises_error(self, cache):
        """测试重复添加应报错"""
        cache.add("p1", "描述")
        with pytest.raises(ValueError, match="已存在"):
            cache.add("p1", "另一个描述")

    def test_merge_persons(self, cache):
        """测试人物合并"""
        cache.add("p1", "描述A")
        cache.add("p2", "描述B")  # p2 > p1
        cache.merge("p1", "p2")
        # p1 应指向 p2
        root = cache.union_find.find("p1_2025-12-24")
        assert root == "p2_2025-12-24"

    def test_get_for_prompt_format(self, cache):
        """测试提示词格式输出"""
        cache.add("p1", "描述A", "user_001")
        main_records, aliases = cache.get_for_prompt()
        assert len(main_records) == 1
        assert main_records[0]["person_id"] == "p1"  # 无日期后缀
        assert main_records[0]["user_id"] == "user_001"
```

```python
# tests/unit/context/test_event_context.py
import pytest
from datetime import datetime
from unittest.mock import mock_open, patch
from context.event_context import EventContext

class TestEventContext:
    """事件上下文测试"""

    @pytest.fixture
    def mock_events_data(self):
        """模拟事件日志数据"""
        return """{"event_id": "evt_00001", "start_time": "2025-12-24T10:00:00", "event_type": "person", "raw_text": "测试事件1", "structured": {"person_ids": ["p1"]}}
{"event_id": "evt_00002", "start_time": "2025-12-24T10:05:00", "event_type": "equipment-only", "raw_text": "测试事件2", "structured": {"equipment": "离心机"}}
"""

    def test_get_recent_events(self, mock_events_data):
        """测试获取最近事件"""
        with patch("builtins.open", mock_open(read_data=mock_events_data)):
            with patch.object(Path, "exists", return_value=True):
                context = EventContext("dummy_path.jsonl")
                events = context.get_recent_events(n=2)

        assert len(events) == 2
        assert events[0]["event_id"] == "evt_00002"  # 最新的在前
        assert events[1]["event_id"] == "evt_00001"

    def test_get_max_event_id_number(self, mock_events_data):
        """测试获取最大事件编号"""
        with patch("builtins.open", mock_open(read_data=mock_events_data)):
            with patch.object(Path, "exists", return_value=True):
                context = EventContext("dummy_path.jsonl")
                max_id = context.get_max_event_id_number()

        assert max_id == 2
```

```python
# tests/unit/video_processing/test_processor_response_parser.py
import pytest
from video_processing.qwen3_vl_flash_processor import Qwen3VLFlashProcessor
from storage.models import VideoSegment

class TestProcessorResponseParser:
    """测试大模型响应解析"""

    @pytest.fixture
    def processor(self):
        # 使用 mock API key，不实际调用
        with pytest.mock.patch.dict("os.environ", {"DASHSCOPE_API_KEY": "mock_key"}):
            return Qwen3VLFlashProcessor(
                api_key="mock_key",
                enable_thinking=False
            )

    def test_parse_dynamic_response_valid_json(self, processor):
        """测试解析有效的模型响应"""
        response = '''
        ```json
        {
          "events_to_append": [
            {
              "event_id": "evt_00001",
              "start_time": "2025-12-24T10:00:00",
              "end_time": "2025-12-24T10:00:10",
              "event_type": "person",
              "person_ids": ["p1"],
              "description": "人物 p1 进入画面"
            }
          ],
          "appearance_updates": [],
          "emergency_events": []
        }
        ```
        '''
        segment = VideoSegment(segment_id="test_001", video_path="/tmp/test.mp4", start_time=0, end_time=10)
        result = processor._parse_dynamic_response(response, segment)

        assert len(result.events) == 1
        assert result.events[0].event_id == "evt_00001"
        assert result.events[0].event_type == "person"
```

**P1 - 高优先级（基础设施）**：

```python
# tests/unit/storage/test_models.py
from storage.models import VideoSegment, EventLog
from datetime import datetime

class TestVideoSegment:
    def test_segment_creation(self):
        segment = VideoSegment(
            segment_id="20251224_120000_01",
            video_path="/tmp/test.mp4",
            start_time=0.0,
            end_time=60.0,
            qr_results=[{"user_id": "u001", "confidence": 0.95}]
        )
        assert segment.duration == 60.0

class TestEventLog:
    def test_event_log_creation(self):
        event = EventLog(
            event_id="evt_00001",
            event_type="person",
            start_time=datetime(2025, 12, 24, 10, 0, 0),
            end_time=datetime(2025, 12, 24, 10, 0, 10),
            raw_text="测试事件"
        )
        assert event.duration_seconds == 10.0
```

**P2 - 中优先级（API 层）**：

```python
# tests/integration/test_web_api.py
import pytest
from fastapi.testclient import TestClient
from web_api.main import app

client = TestClient(app)

class TestAuthAPI:
    """测试认证接口"""

    def test_register_and_login(self):
        """测试注册和登录流程"""
        # 注册
        response = client.post("/auth/register", json={
            "username": "testuser",
            "password": "testpass123",
            "email": "test@example.com"
        })
        assert response.status_code == 200

        # 登录
        response = client.post("/auth/login", data={
            "username": "testuser",
            "password": "testpass123"
        })
        assert response.status_code == 200
        assert "access_token" in response.json()
```

**阶段三：Android 端测试引入**

```kotlin
// android-app-trial/app/src/test/java/com/example/lablogcamera/utils/OcrBFontRendererTest.kt
package com.example.lablogcamera.utils

import org.junit.Test
import org.junit.Assert.*

class OcrBFontRendererTest {

    @Test
    fun `test timestamp formatting`() {
        val timestamp = 1703414400000L // 2023-12-24 12:00:00
        val result = OcrBFontRenderer.formatTimestamp(timestamp)
        assertEquals("Time: 12:00:00", result)
    }

    @Test
    fun `test character bitmap generation`() {
        val renderer = OcrBFontRenderer()
        val bitmap = renderer.getCharacterBitmap('5', width = 20, height = 30)
        assertNotNull(bitmap)
        assertEquals(20, bitmap.width)
        assertEquals(30, bitmap.height)
    }
}
```

```kotlin
// android-app-trial/app/src/test/java/com/example/lablogcamera/utils/VideoEncoderTest.kt
package com.example.lablogcamera.utils

import org.junit.Test
import org.junit.runner.RunWith
import org.robolectric.RobolectricTestRunner

@RunWith(RobolectricTestRunner::class)
class VideoEncoderTest {

    @Test
    fun `test encoder initialization`() {
        val encoder = VideoEncoder(preferH265 = true) { _, _ -> }
        // 验证初始化参数
        assertTrue(encoder.preferH265)
    }
}
```

**阶段四：持续集成（GitHub Actions）**

`.github/workflows/test.yml`：
```yaml
name: Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test-python:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install -e ".[dev]"

    - name: Run tests with coverage
      run: |
        pytest --cov=. --cov-report=xml --cov-report=term

    - name: Upload coverage
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml

  test-android:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Run Android unit tests
      run: |
        cd android-app-trial
        ./gradlew test
```

#### 测试实施路线图

| 阶段 | 内容 | 预计时间 | 产出 |
|------|------|----------|------|
| **Week 1** | 搭建测试基础设施 | 1-2 天 | pytest 配置、CI 流水线、目录结构 |
| **Week 2** | 核心模块单元测试 | 3-4 天 | context/ 模块 80%+ 覆盖率 |
| **Week 3** | API 层测试 | 2-3 天 | Web API 关键路径覆盖 |
| **Week 4** | Android 单元测试 | 3-4 天 | 关键工具类测试 |
| **持续** | 新功能 TDD | - | 新代码同步编写测试 |

#### 关键建议

**1. 测试策略优先级**

```
单元测试 (70%) > 集成测试 (20%) > E2E 测试 (10%)
```

- **单元测试**：测试纯逻辑（如外貌缓存、事件解析），不依赖外部服务
- **集成测试**：测试模块间协作（如处理器 + 数据库）
- **E2E 测试**：仅覆盖核心业务流程（如完整录制→处理→存储）

**2. Mock 外部依赖**

```python
# 使用 pytest-mock 隔离外部服务
@pytest.fixture
def mock_dashscope(mocker):
    """Mock DashScope API 调用"""
    return mocker.patch(
        'dashscope.MultiModalConversation.call',
        return_value={
            "output": {
                "choices": [{
                    "message": {
                        "content": [{"text": "mock response"}]
                    }
                }]
            }
        }
    )
```

**3. 测试数据管理**

```python
# tests/conftest.py
import pytest
from pathlib import Path

@pytest.fixture(scope="session")
def test_data_dir():
    """测试数据目录"""
    return Path(__file__).parent / "fixtures"

@pytest.fixture
def sample_video_segment(test_data_dir):
    """提供样本视频分段数据"""
    return VideoSegment(
        segment_id="test_001",
        video_path=str(test_data_dir / "sample_10s.mp4"),
        start_time=0,
        end_time=10
    )
```

**4. 覆盖率门槛**

建议设定以下 CI 门槛：
```yaml
# .github/workflows/test.yml 补充
- name: Check coverage threshold
  run: |
    pytest --cov=. --cov-fail-under=60  # 初始门槛 60%，逐步提升至 80%
```

**5. 现有脚本改造**

将现有的 `test_*.py` 脚本改造为真正的测试：

```python
# 改造前: scripts/test_segmentation.py（需要手动传入视频路径）
# 改造后: tests/integration/test_segmentation.py

@pytest.mark.integration
@pytest.mark.skipif(
    not os.getenv("TEST_VIDEO_PATH"),
    reason="需要设置 TEST_VIDEO_PATH 环境变量"
)
def test_video_segmentation():
    """集成测试：视频分段"""
    segmenter = VideoSegmenter(use_temporary_files=True)
    segments = segmenter.segment(os.getenv("TEST_VIDEO_PATH"))

    assert len(segments) > 0
    # 验证分段连续性
    for i in range(len(segments) - 1):
        assert segments[i].end_time <= segments[i+1].start_time + 1.0
```

**总结**：当前项目测试覆盖几乎为零，建议立即投入 1-2 周时间搭建测试基础设施并为核心业务模块（context/、video_processing/）编写单元测试。这将显著降低后续重构风险，并为引入 YOLO 等新功能提供安全保障。

---

### 7.6 人物形象动态上下文维护的代码现状梳理

#### 整体流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              客户端 (Android)                                │
│                                                                             │
│  1. 二维码识别 (ML Kit)                                                      │
│     └─► processQrCode() 解析二维码内容 → QrDetection(userId, content)        │
│                                                                             │
│  2. 缓存聚合                                                                 │
│     └─► qrCache: ConcurrentHashMap<String, QrDetection>                     │
│         以 userId 为 key，同一人多次扫码会更新置信度和时间                      │
│                                                                             │
│  3. 随分段上报                                                               │
│     └─► sendMP4Segment() 将 qrCache.values 转为 JSON 数组                   │
│         qr_results: [{user_id, detected_at_ms, detected_at, confidence}]    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼ WebSocket
┌─────────────────────────────────────────────────────────────────────────────┐
│                              后端 (Python)                                   │
│                                                                             │
│  4. 接收并保存                                                               │
│     └─► streaming_server/server.py:handle_mp4_segment()                     │
│         保存为 {segment_id}_qr.json                                         │
│                                                                             │
│  5. 构建提示词                                                               │
│     └─► context/prompt_builder.py:_format_qr_results()                      │
│         格式化："- 用户ID: xxx, 识别时间: xxx"                                │
│                                                                             │
│  6. 提示词注入 + 绑定规则                                                     │
│     └─► PromptBuilder.build_dynamic_prompt()                                │
│         "### 4. 二维码用户关联"                                             │
│         "- 当二维码识别结果中的时间戳与视频画面时间接近时..."                  │
│         "- 如果该人物的外貌与已有外貌记录匹配，则更新该外貌记录，补充 user_id"│
│         "- 如果该人物的外貌与已有外貌记录不匹配，则追加新记录，带有 user_id"  │
│                                                                             │
│  7. 大模型推理                                                               │
│     └─► Qwen3-VL 分析视频 + 上下文，输出 appearance_updates                 │
│                                                                             │
│  8. 应用更新                                                                 │
│     └─► appearance_cache.add/update/merge()                                 │
│         更新内存中的外貌表，包含 user_id 字段                                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

#### 关键代码详解

##### 1. 客户端二维码识别与缓存

**数据结构** (`OriginalMainActivity.kt:196-201`):
```kotlin
data class QrDetection(
    val userId: String,           // 从二维码内容解析的 user_id
    val content: String,          // 原始二维码内容 (JSON)
    val confidence: Float,        // 识别置信度
    val detectedAtMs: Long        // 识别时间戳
)
```

**二维码解析** (`OriginalMainActivity.kt:2165-2194`):
```kotlin
// 二维码内容格式：{"user_id": "u123", "public_key_fingerprint": "fp_xxx"}
val parsed = JSONObject(qr.content)
val uid = parsed.optString("user_id")
val fp = parsed.optString("public_key_fingerprint")
val dedupKey = if (uid.isNotBlank() || fp.isNotBlank()) {
    "uid=$uid|fp=$fp"  // 去重 key
} else {
    content
}

// 存入缓存，相同 key 置信度更高则替换
qrCache.compute(dedupKey) { _, prev ->
    val shouldReplace = prev == null || confidence > prev.confidence
    if (shouldReplace) {
        QrDetection(
            userId = dedupKey,
            content = content,
            confidence = confidence,
            detectedAtMs = detectedAt
        )
    } else {
        prev
    }
}
```

**随分段上报** (`OriginalMainActivity.kt:1914-1951`):
```kotlin
val qrArray = JSONArray()
val qrSnapshot = qrCache.values.toList()  // 取当前缓存的所有二维码
qrSnapshot.forEach { qr ->
    val obj = JSONObject()
        .put("confidence", qr.confidence)
        .put("detected_at_ms", qr.detectedAtMs)
        .put("detected_at", detectedIso)
    // 解析二维码 JSON 提取 user_id 和 fingerprint
    val parsed = JSONObject(qr.content)
    parsed.optString("user_id").let { obj.put("user_id", it) }
    parsed.optString("public_key_fingerprint").let { obj.put("public_key_fingerprint", it) }
    qrArray.put(obj)
}

// 随 mp4_segment 消息一起发送
val message = JSONObject().apply {
    put("type", "mp4_segment")
    put("qr_results", qrArray)  // <-- 二维码结果在这里
}
webSocket?.send(message)

// 发送后清空缓存，准备下一分段
resetQrState()  // qrCache.clear()
```

---

##### 2. 后端绑定逻辑

**二维码格式化进提示词** (`prompt_builder.py:108-111, 168-191`):
```python
def _format_qr_results(self, qr_results: List[Dict[str, Any]]) -> str:
    if not qr_results:
        return "（本视频片段无二维码识别结果）"

    lines = []
    for qr in qr_results:
        user_id = qr.get('user_id', '未知')
        detected_at = qr.get('detected_at', '')
        lines.append(f"- 用户ID: {user_id}, 识别时间: {detected_at}")

    return "\n".join(lines)

# 在 build_dynamic_prompt 中使用：
prompt = f"""...
## 二维码识别结果
{qr_section}  # <-- 格式化后的二维码列表

## 人物外貌表（已全量给出）
...

### 4. 二维码用户关联
- 当二维码识别结果中的时间戳与视频画面时间接近时，观察该时刻画面中展示二维码的人物
- 如果该人物的外貌与已有外貌记录匹配，则更新该外貌记录，补充 user_id
- 如果该人物的外貌与已有外貌记录不匹配，则追加新记录，带有 user_id
...
"""
```

**外貌缓存结构** (`appearance_cache.py:11-18`):
```python
@dataclass
class AppearanceRecord:
    person_id: str              # p1, p2...
    appearance: str             # 外貌描述
    user_id: Optional[str] = None  # <-- 关联的用户ID（来自二维码）
```

**外貌表更新** (`appearance_cache.py:129-151`):
```python
def update(self, person_id: str, appearance: Optional[str] = None,
           user_id: Optional[str] = None) -> None:
    """
    更新人物记录（可补充外貌描述、补全 user_id）
    """
    full_id = self._get_full_id(person_id)
    root_id = self.union_find.find(full_id)

    if root_id not in self.records:
        raise ValueError(f"人物编号不存在")

    record = self.records[root_id]
    if appearance is not None:
        record.appearance = appearance
    if user_id is not None:
        record.user_id = user_id  # <-- 补充 user_id

def add(self, person_id: str, appearance: str, user_id: Optional[str] = None):
    # 新增人物时可以携带 user_id
    self.records[full_id] = AppearanceRecord(
        person_id=full_id,
        appearance=appearance,
        user_id=user_id  # <-- 新增时就有 user_id
    )
```

**合并时的 user_id 继承** (`appearance_cache.py:175-181`):
```python
def merge(self, merge_from: str, target_person_id: str):
    # ...
    # 如果被合并的记录有 user_id 但目标没有，则继承
    from_root = self.union_find.find(full_from)
    if full_from in self.records and from_root in self.records:
        from_record = self.records[full_from]
        to_record = self.records[from_root]
        if from_record.user_id and not to_record.user_id:
            to_record.user_id = from_record.user_id  # <-- 合并时继承
```

---

##### 3. 大模型输出示例

模型根据提示词中的"二维码用户关联"规则，输出 `appearance_updates`:

```json
{
  "events_to_append": [
    {
      "event_id": "evt_00001",
      "description": "人物 p3 向摄像头展示二维码"
    }
  ],
  "appearance_updates": [
    {
      "op": "update",           // 或 "add"
      "target_person_id": "p3",
      "appearance": "短黑发，戴眼镜，穿白色实验服",
      "user_id": "uid=zhangsan|fp=abc123"  // <-- 从二维码获取的 user_id
    }
  ]
}
```

---

#### 绑定流程总结

| 步骤 | 代码位置 | 关键操作 |
|------|----------|----------|
| 1. 扫码 | `OriginalMainActivity.kt:2165` | ML Kit 识别二维码 JSON |
| 2. 缓存 | `OriginalMainActivity.kt:1256` | `qrCache[uid] = QrDetection` |
| 3. 上报 | `OriginalMainActivity.kt:1944` | `put("qr_results", qrArray)` |
| 4. 提示 | `prompt_builder.py:61-111` | 二维码信息注入提示词 |
| 5. 推理 | `qwen3_vl_flash_processor.py:312` | 大模型分析并输出绑定 |
| 6. 更新 | `appearance_cache.py:150` | `record.user_id = user_id` |

---

#### 存在的问题与思考

1. **绑定时机依赖大模型**：二维码识别后，需要等大模型处理视频分段才能完成绑定，延迟较大（分钟级）。若需实时绑定，需考虑客户端直接上报或后端预绑定机制。

2. **多段视频中的连续性**：如果同一个人在不同分段展示二维码，后端需要正确识别为同一人并更新外貌表。当前依靠外貌描述匹配，可能存在误判。

3. **user_id 格式不统一**：代码中既有 `uid=xxx|fp=xxx` 这样的 dedupKey，也有纯 `user_id`，可能导致混淆。建议统一格式。

4. **合并时的继承逻辑**：当前只有 `from` 有而 `target` 没有时才继承，如果两者都有不同值，没有处理冲突。需明确冲突解决策略。

5. **没有验证机制**：任何人可以展示任何人的二维码，没有身份验证。若用于安全敏感场景，需增加身份验证环节。

---

---

### 7.7 视频采集数据的本地存储与清理机制

#### 问题描述

当前系统中，Android 客户端采集的视频数据通过 WebSocket 推送到后端流媒体服务器后，会以 MP4 文件形式**永久保存在服务器本地磁盘**上，存储路径为 `recordings/<timestamp>/` 目录。每个录制会话产生一个独立目录，包含视频分段文件、二维码识别结果和缩略图。

**关键代码位置**：
- 保存逻辑：`streaming_server/server.py` (`handle_mp4_segment`)
- 存储路径定义：`streaming_server/server.py` (`RECORDINGS_ROOT = Path("recordings")`)

##### 问题分析

**当前存储行为**：

| 数据类型 | 存储位置 | 清理策略 | 风险 |
|---------|---------|---------|------|
| MP4 视频分段 | `recordings/<session>/` | ❌ 无自动清理 | 磁盘空间持续增长 |
| 二维码结果 | 同上 | ❌ 无自动清理 | 同上 |
| 缩略图 | 同上 | ❌ 无自动清理 | 同上 |

**风险场景**：

| 场景 | 影响 |
|------|------|
| 长期运行 | 磁盘空间耗尽，服务崩溃 |
| 多路并发 | 空间增长加速 |
| 无告警机制 | 被动发现问题 |

**根本问题**：
- 视频数据在完成视频理解和事件提取后，原始 MP4 文件失去了直接业务价值
- 系统缺乏生命周期管理策略
- 没有磁盘监控和告警机制

---

#### 解决方案

##### 方案一：服务端基于时间的自动清理策略

**核心思路**：

在服务端实施分层存储和自动清理机制，根据数据年龄决定保留策略：

```
视频数据生命周期：
┌─────────────┬─────────────┬─────────────┬─────────────┐
│   0-7 天    │   7-30 天   │  30-90 天   │   >90 天    │
│  (热数据)    │  (温数据)    │  (冷数据)    │  (归档/删除) │
├─────────────┼─────────────┼─────────────┼─────────────┤
│ 保留完整数据 │ 仅保留缩略图 │ 仅保留元数据 │  完全删除   │
│ 支持视频回放 │ 快速预览    │ 事件可追溯   │ 释放空间    │
└─────────────┴─────────────┴─────────────┴─────────────┘
```

**实施要点**：
- 开发定时清理脚本，按配置策略删除或精简过期数据
- 配置 systemd 定时任务每日执行
- 增加磁盘空间监控，超过阈值时发送告警

**适用场景**：对存储成本敏感、无需长期保留原始视频的业务场景

---

##### 方案二：对象存储迁移

**核心思路**：

将视频数据从本地磁盘迁移到对象存储（如 MinIO、阿里云 OSS），利用对象存储的生命周期管理能力：

- 热数据本地缓存，冷数据自动归档到低成本存储
- 利用对象存储的生命周期策略自动删除过期数据
- 支持多服务器共享存储，弹性扩容

**实施要点**：
- 部署或接入对象存储服务
- 修改服务端保存逻辑，上传成功后删除本地文件
- 视频理解时通过预签名 URL 访问对象存储中的视频

**适用场景**：需要长期归档、多节点共享存储、有对象存储基础设施的场景

---

##### 方案三：服务端处理后将数据回推至采集端本地存储

**核心思路**：

将存储责任从服务端转移到采集端，形成"服务端临时处理、采集端长期归档"的架构：

```
Day 0 (采集当天)：
Android 采集端 → 推流到服务端 → 实时视频理解 → 提取事件日志
                                    ↓
                              服务端保留原始视频

Day 1 (次日)：
服务端定时任务启动 ──→ 将昨日视频数据打包
                              ↓
                    通过 WebSocket/HTTP 回传给采集端
                              ↓
                    采集端本地保存到指定目录
                              ↓
                    服务端删除已回传的数据

Day 7 (每周)：
采集端定时清理本地存储，删除超过 7 天的视频数据
```

**实施要点**：

1. **服务端回推机制**：
   - 开发定时任务，每日凌晨将昨日录制的视频数据打包
   - 通过现有 WebSocket 通道或新建 HTTP 接口将数据回传给采集端
   - 采集端确认接收成功后，服务端删除本地副本

2. **采集端接收与存储**：
   - 扩展 Android 客户端，增加"接收服务端回传数据"的能力
   - 将接收到的视频保存到本地存储（如 SD 卡或指定目录）
   - 提供本地视频浏览和管理界面

3. **采集端定期清理**：
   - 配置每周定时清理任务
   - 删除本地存储中超过 7 天的视频数据

**优势**：
- **分散存储压力**：服务端仅需保留 1 天数据，大幅降低服务器存储成本
- **利用边缘存储**：利用采集端设备的本地存储能力
- **数据主权**：原始视频保留在采集端，符合某些场景的隐私要求

**挑战**：
- **采集端存储限制**：手机/设备存储容量有限，无法长期保留大量视频
- **回传可靠性**：需要处理网络中断、采集端离线等异常情况
- **复杂性增加**：需要维护服务端和采集端的双向数据传输逻辑

**适用场景**：
- 采集端设备存储空间充足（如专用平板、带 SD 卡的设备）
- 服务端存储成本敏感
- 对原始视频的访问频率较低（主要用于事后追溯）

---

#### 实施建议

| 优先级 | 方案 | 实施难度 | 预期效果 | 推荐时机 |
|--------|------|---------|---------|---------|
| P0 | 方案一（服务端定时清理） | 低 | 防止磁盘耗尽 | 立即实施 |
| P1 | 磁盘监控告警 | 低 | 主动发现问题 | 与 P0 同步 |
| P2 | 方案三（回推采集端） | 中 | 分散存储压力 | 有专用采集设备时 |
| P3 | 方案二（对象存储） | 中 | 长期可扩展 | 存储需求持续增长 |

**推荐实施路径**：

```
Phase 1（本周）：
├── 实现服务端定时清理脚本（保留 7 天策略）
├── 配置定时任务每日执行
└── 添加磁盘空间监控告警

Phase 2（评估后）：
├── 评估采集端设备存储能力和网络稳定性
├── 如条件满足，实施方案三（回推采集端）
└── 否则考虑方案二（对象存储）

Phase 3（持续）：
├── 根据实际运行情况调整保留策略
└── 优化监控和告警阈值
```

---

*文档生成时间：2026-03-22*
*基于代码版本：main 分支*
